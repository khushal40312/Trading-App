version: '3.8'
services:
  server:
    build: ./Backend
    ports:
      - "5000:5000"
    environment:
      - MONGO_URL=mongodb+srv://yograjvashist100:Anu40312@cluster0.su0yw0s.mongodb.net/
    depends_on:
      - mongo

  client:
    build: ./Client
    ports:
      - "5173:80"

  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
